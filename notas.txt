python -m venv entorno_django
source entorno_django/Scripts/activate
pip install django
django-admin --help
django-admin startproject libreria   < startproject srive para crear el proyecto y "libreria" es el nombre del pryecto
python manage.py runserver < iniciara el servidro de django
python manage.py showmigrations < mostrara todas las migracione en nuestro proyecto y con [x] las que nya se ejecutaron en la db
python manage.py migrate < srive para ejecutar las migraciones faltantes en neustro proyecto
python manage.py startapp [nombre de app] < crea una palicacion en nuestro proyecto
python manage.py makemigrations [nombre de aplicacion] -n "[comentario]" < crea la migracion en la aplicacion indicada