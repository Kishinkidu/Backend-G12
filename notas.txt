python -m venv entorno_django
source entorno_django/Scripts/activate
pip install django
django-admin --help
django-admin startproject [nombre del proyecto] .   < startproject srive para crear el proyecto y "libreria" es el nombre del pryecto
>>>>> el "." hace que cree la carpeta en el mismo nivel , en la cual nos ubicamos.
python manage.py runserver < iniciara el servidro de django
python manage.py showmigrations < mostrara todas las migracione en nuestro proyecto y con [x] las que nya se ejecutaron en la db
python manage.py migrate < srive para ejecutar(correr) las migraciones faltantes en neustro proyecto
python manage.py startapp [nombre de app] < crea una palicacion en nuestro proyecto
python manage.py makemigrations [nombre de aplicacion] -n "[nombre]" < CREA la MIGRACION en la aplicacion indicada


opc. python manage.py makemigrations [nombre de aplicacion] --name [nombre del proyecto]

1: python manage.py makemigrations [nombre de aplicacion] -n "[comentario]" < CREA la MIGRACION en la aplicacion indicada
2: python manage.py migrate < srive para ejecutar(correr) las migraciones faltantes en neustro proyecto
3: python manage.py showmigrations < mostrara todas las migracione en nuestro proyecto y con [x] las que nya se ejecutaron en la db 
